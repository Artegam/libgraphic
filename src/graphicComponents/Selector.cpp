#include "GraphicComponents.h"

using namespace GraphicComponents;


Selector::Selector (const int window, const int x, const int y, const string label, const string name) : Text(window, x, y, label, name) {
}
